ccxt>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0
python-dotenv>=1.0.0
aiosqlite>=0.19.0
pyjwt>=2.8.0
httpx>=0.24.0

# 風控 / 行事曆 YAML 解析（market_hours, broker_config）
pyyaml>=6.0

# 永豐證券 Shioaji（僅 prod 機需要；dev 機可不裝，broker factory 會 fallback 為 VirtualBroker）
# ⚠️ 必須裝官方 shioaji（PyPI 名稱: shioaji），不要裝 rshioaji（社群 Rust fork、非永豐官方、客服不支援）
# 2026-06-24 mini 上發現誤裝 rshioaji 1.5.13、已移除並改回官方 shioaji 1.5.3
# 詳見 memory/project_rshioaji_cleanup_2026_06_24
# pip install shioaji==1.5.3
# shioaji==1.5.3

# ── Dev / Test ──
pytest>=8.0.0
